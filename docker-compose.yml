version: '3.8'

services:
  # Data Layer Service
  data-service:
    build:
      context: ./backend/booking-data
      dockerfile: Dockerfile
    env_file:
      - ./backend/booking-data/.env
    ports:
      - "3002:3002"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Business Logic Service
  booking-logic:
    build:
      context: ./backend/booking-logic
      dockerfile: Dockerfile
    env_file:
      - ./backend/booking-logic/.env
    ports:
      - "3001:3001"
    depends_on:
      data-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
  
  # Paypal Adapter Service
  paypal-adapter:
    build:
        context: ./backend/paypal-adapter
        dockerfile: Dockerfile
    env_file:
      - ./backend/paypal-adapter/.env
    ports:
      - "3003:3003"
    depends_on:
      data-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
  
  # Oauth Adapter Service
  oauth-adapter:
    build:
      context: ./backend/oauth-adapter
      dockerfile: Dockerfile
    env_file:
      - ./backend/oauth-adapter/.env
    ports:
      - "3004:3004"
    depends_on:
      data-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
  
  # Orchestrator Service
  booking-orchestrator:
    build:
      context: ./backend/booking-orchestrator
      dockerfile: Dockerfile
    env_file:
      - ./backend/booking-orchestrator/.env
    ports:
      - "3000:3000"
    depends_on:
      booking-logic:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
  # Frontend Service 
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_HOST=0.0.0.0
      # Points backed API to backend service name (Docker DNS)
      - VITE_API_URL=http://booking-orchestrator:3000/api/v1
    env_file:
      - ./frontend/.env
    depends_on:
      booking-orchestrator:
        condition: service_started

  # Booking Cleanup Cron Job
  booking-cleanup-cron:
    build: ./backend/booking-cleanup-cron
    environment:
      - CLEANUP_API_URL=http://data-service:3002/api/v1/reservations/cleanup
      - CLEANUP_API_KEY=your-internal-api-key # Optional, if you secure the endpoint
    depends_on:
      data-service:
        condition: service_healthy

  # API Documentation Server
  api-docs-server:
    build: ./api-docs
    ports:
      - "4000:4000"
    depends_on:
      - booking-orchestrator
      - booking-logic
      - data-service
      - paypal-adapter
      - oauth-adapter