const bookingSettings = {
    reservationExpirationMinutes: 15, // Reservations expire after 15 minutes
    maxConcurrentReservationPerUser: 1, // Max 1 active reservation per user
}

module.exports = bookingSettings
