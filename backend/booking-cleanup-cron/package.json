{
  "name": "booking-cleanup-cron",
  "version": "1.0.0",
  "description": "Cron job to trigger reservation cleanup via API.",
  "main": "cleanupCron.js",
  "scripts": {
    "start": "node cleanupCron.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "node-cron": "^3.0.2"
  }
}
